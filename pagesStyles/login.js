import styled from 'styled-components';

const LoginBoxWrapper = styled.div`
  display: flex;
  justify-content: center;
  background-color: green;
`;

const Footer = styled.footer``;

export { LoginBoxWrapper, Footer };
